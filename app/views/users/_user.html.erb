<div  class="col-md-3 card">
  <br>
  <%= gravatar_for user %>
<h3><%= user.name %></h3>
<h2><%= user.email %></h2>
<% puts added(@slot, user) %>
<%puts "function caldled"%>
<%if !added(@slot, user)%>
<%=link_to "Add User", addtoslot_path(:slot_id => params[:slot_id], :user_id => user.id), method: :post, class:"btn btn-success btn-md" %>
<%else%>
<%rec= @slot.srecord.where(user_id: user.id).first%>
<p>Status:</p>
<%=rec.status%>
 <%end%>
<br>
<br>
</div>
